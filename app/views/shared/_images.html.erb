<% @images.each do |image| %>
  <tr>
    <td><%= image_tag image.data.url(:thumb), class: "img-rounded"%></td>
    <td><%= image.title %></td>
    <td><%= image.description %></td>
    <td>
      <% if @category_array.map { |category| [category.id = image.category_id] } %>
        <%= link_to("#{image.category.title}", lists_path(:category_id => image.category_id)) %>
      <% end %>
    </td>
      <td><%= link_to 'Show', image %></td>
      <td><%= link_to 'Edit', edit_image_path(image) if signed_in?%></td>
      <td><%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Удалить?' } if signed_in?%></td>
  </tr>
<% end %>
