<%= form_for(@select) do |f| %>
  <% if @select.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@select.errors.count, "error") %> prohibited this select from being saved:</h2>

      <ul>
      <% @select.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :on %><br />
    <%= f.check_box :on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
